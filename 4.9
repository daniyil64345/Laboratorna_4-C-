#include <iostream>
using namespace std;

int main() {
    int a, b, c, d;
    cout << "Enter your four integers:\n";
    cin >> a >> b >> c >> d;
    if ((a > 1 && a < 255) &&
        (b > 1 && b < 255) &&
        (c > 1 && c < 255) &&
        (d > 1 && d < 255)) {

        cout << "Your IP address is: " << a << "." << b << "." << c << "." << d << endl;
    }
    else {
        cout << "Some invalid integer. You need to write numbers between 1 and 255" << endl;
    }
}
